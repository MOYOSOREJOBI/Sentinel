//line /Users/mac/Desktop/Sentinel/internal/query/scores.go:1:1
package query

import (
	"context"
	"fmt"
	"time"

	"github.com/jackc/pgx/v5/pgxpool"
)

// ScorePoint is a single data point in the score time series.
type ScorePoint struct {
	Time              time.Time `json:"time"`
	Ts                time.Time `json:"ts"`
	AnomalyScore      float64   `json:"anomaly_score"`
	ScoreNorm         float64   `json:"score_norm"`
	ScoreRaw          float64   `json:"score_raw"`
	EscalationProb    float64   `json:"escalation_prob"`
	Composite         float64   `json:"composite"`
	CompositeRisk     float64   `json:"composite_risk"`
	ModelVersion      string    `json:"model_version"`
	ProducedAt        time.Time `json:"produced_at"`
	ScoringRunID      string    `json:"scoring_run_id"`
	FeatureSnapshot   string    `json:"feature_snapshot_hash"`
	ArtifactHash      string    `json:"artifact_hash"`
	ModelArtifactHash string    `json:"model_artifact_hash"`
	Symbol            string    `json:"symbol"`
	Severity          string    `json:"severity"`
}

// CandlePoint is one OHLCV bucket for the chart.
type CandlePoint struct {
	Ts     time.Time `json:"ts"`
	Open   float64   `json:"open"`
	High   float64   `json:"high"`
	Low    float64   `json:"low"`
	Close  float64   `json:"close"`
	Volume float64   `json:"volume"`
	Symbol string    `json:"symbol"`
}

func windowInterval(window string) string {goCover_fbd601c42ea1__17[0] = 9 ; goCover_fbd601c42ea1__17[1] = goCover_fbd601c42ea1_P ; goCover_fbd601c42ea1__17[2] = 17 ; goCover_fbd601c42ea1__17[3] = 1;
	switch window {
	case "1h":goCover_fbd601c42ea1__17[4] = 1;
		return "1 hour"
	case "7d":goCover_fbd601c42ea1__17[5] = 1;
		return "7 days"
	case "30d":goCover_fbd601c42ea1__17[6] = 1;
		return "30 days"
	case "90d":goCover_fbd601c42ea1__17[7] = 1;
		return "90 days"
	case "1y":goCover_fbd601c42ea1__17[8] = 1;
		return "1 year"
	case "5y":goCover_fbd601c42ea1__17[9] = 1;
		return "5 years"
	case "20y":goCover_fbd601c42ea1__17[10] = 1;
		return "20 years"
	default:goCover_fbd601c42ea1__17[11] = 1;
		return "24 hours"
	}
}

func candleResolution(res string) string {goCover_fbd601c42ea1__18[0] = 5 ; goCover_fbd601c42ea1__18[1] = goCover_fbd601c42ea1_P ; goCover_fbd601c42ea1__18[2] = 18 ; goCover_fbd601c42ea1__18[3] = 1;
	switch res {
	case "5m":goCover_fbd601c42ea1__18[4] = 1;
		return "5 minutes"
	case "1h":goCover_fbd601c42ea1__18[5] = 1;
		return "1 hour"
	case "1d":goCover_fbd601c42ea1__18[6] = 1;
		return "1 day"
	default:goCover_fbd601c42ea1__18[7] = 1;
		return "1 minute"
	}
}

func scoreSeriesSource(window string) (string, string) {goCover_fbd601c42ea1__19[0] = 4 ; goCover_fbd601c42ea1__19[1] = goCover_fbd601c42ea1_P ; goCover_fbd601c42ea1__19[2] = 19 ; goCover_fbd601c42ea1__19[3] = 1;
	switch window {
	case "1y", "5y", "20y":goCover_fbd601c42ea1__19[4] = 1;
		return "scores_1d", "bucket"
	case "30d", "90d":goCover_fbd601c42ea1__19[5] = 1;
		return "scores_1h", "bucket"
	default:goCover_fbd601c42ea1__19[6] = 1;
		return "scores", "ts"
	}
}

func candleSeriesSource(window string) (string, string, bool) {goCover_fbd601c42ea1__20[0] = 4 ; goCover_fbd601c42ea1__20[1] = goCover_fbd601c42ea1_P ; goCover_fbd601c42ea1__20[2] = 20 ; goCover_fbd601c42ea1__20[3] = 1;
	switch window {
	case "1y", "5y", "20y":goCover_fbd601c42ea1__20[4] = 1;
		return "candles_1d", "bucket", true
	case "30d", "90d":goCover_fbd601c42ea1__20[5] = 1;
		return "candles_1h", "bucket", true
	default:goCover_fbd601c42ea1__20[6] = 1;
		return "candles", "bucket", false
	}
}

func scoreWindowPredicate(window string, from, to time.Time, startArg int, timeCol string) (string, []any) {goCover_fbd601c42ea1__21[0] = 3 ; goCover_fbd601c42ea1__21[1] = goCover_fbd601c42ea1_P ; goCover_fbd601c42ea1__21[2] = 21 ; goCover_fbd601c42ea1__21[3] = 1;
	if window == "custom" && !from.IsZero() && !to.IsZero() {goCover_fbd601c42ea1__21[5] = 1;
		return fmt.Sprintf("%s BETWEEN $%d AND $%d", timeCol, startArg, startArg+1), []any{from, to}
	}
	goCover_fbd601c42ea1__21[4] = 1;return fmt.Sprintf("%s > now() - interval '%s'", timeCol, windowInterval(window)), nil
}

// LoadScoreSeries returns a time-ordered score series for charting.
// symbol="" returns all symbols; window selects the lookback period.
// Results are sampled to at most maxPoints using a simple nth-row approach.
func LoadScoreSeries(ctx context.Context, db *pgxpool.Pool, symbol, window string, maxPoints int, from, to time.Time) ([]ScorePoint, error) {goCover_fbd601c42ea1__22[0] = 9 ; goCover_fbd601c42ea1__22[1] = goCover_fbd601c42ea1_P ; goCover_fbd601c42ea1__22[2] = 22 ; goCover_fbd601c42ea1__22[3] = 1;
	if maxPoints <= 0 {goCover_fbd601c42ea1__22[7] = 1;
		maxPoints = 500
	}
	goCover_fbd601c42ea1__22[4] = 1;source, timeCol := scoreSeriesSource(window)
	timeWhere, extraArgs := scoreWindowPredicate(window, from, to, 3, timeCol)

	query := fmt.Sprintf(`
		WITH base AS (
			SELECT
				%s AS ts,
				COALESCE(normalized_anomaly_score, score, 0) AS anomaly_score,
				COALESCE(normalized_anomaly_score, score, 0) AS score_norm,
				COALESCE(raw_anomaly_score, score, 0)        AS score_raw,
				COALESCE(escalation_probability, 0)          AS escalation_prob,
				COALESCE(composite_risk, score, 0)           AS composite,
				COALESCE(composite_risk, score, 0)           AS composite_risk,
				COALESCE(model_version, 'baseline-v1')       AS model_version,
				COALESCE(produced_at, ts)                    AS produced_at,
				COALESCE(scoring_run_id::text, '')           AS scoring_run_id,
				COALESCE(feature_snapshot_hash, '')          AS feature_snapshot_hash,
				COALESCE(artifact_hash, '')                  AS artifact_hash,
				COALESCE(model_artifact_hash, '')            AS model_artifact_hash,
				symbol,
				COALESCE(severity, '')                       AS severity,
				row_number() OVER (ORDER BY COALESCE(produced_at, %s)) AS rn,
				count(*) OVER ()                             AS total
			FROM %s
			WHERE %s
			  AND ($1 = '' OR symbol = $1)
		)
		SELECT ts, anomaly_score, score_norm, score_raw, escalation_prob, composite, composite_risk, model_version, produced_at, scoring_run_id, feature_snapshot_hash, artifact_hash, model_artifact_hash, symbol, severity
		FROM base
		WHERE total <= $2 OR rn %% GREATEST(1, total / $2) = 0
		ORDER BY produced_at
		LIMIT $2
	`, timeCol, timeCol, source, timeWhere)

	args := []any{symbol, maxPoints}
	args = append(args, extraArgs...)
	rows, err := db.Query(ctx, query, args...)
	if err != nil {goCover_fbd601c42ea1__22[8] = 1;
		return nil, err
	}
	goCover_fbd601c42ea1__22[5] = 1;defer rows.Close()

	out := make([]ScorePoint, 0, maxPoints)
	for rows.Next() {goCover_fbd601c42ea1__22[9] = 1;
		var p ScorePoint
		if err := rows.Scan(&p.Ts, &p.AnomalyScore, &p.ScoreNorm, &p.ScoreRaw, &p.EscalationProb, &p.Composite, &p.CompositeRisk, &p.ModelVersion, &p.ProducedAt, &p.ScoringRunID, &p.FeatureSnapshot, &p.ArtifactHash, &p.ModelArtifactHash, &p.Symbol, &p.Severity); err != nil {goCover_fbd601c42ea1__22[11] = 1;
			continue
		}
		goCover_fbd601c42ea1__22[10] = 1;p.Time = p.Ts
		out = append(out, p)
	}
	goCover_fbd601c42ea1__22[6] = 1;return out, nil
}

// LoadCandleSeries returns OHLCV candles for the charting panel.
func LoadCandleSeries(ctx context.Context, db *pgxpool.Pool, symbol, window, res string, maxPoints int) ([]CandlePoint, error) {goCover_fbd601c42ea1__23[0] = 12 ; goCover_fbd601c42ea1__23[1] = goCover_fbd601c42ea1_P ; goCover_fbd601c42ea1__23[2] = 23 ; goCover_fbd601c42ea1__23[3] = 1;
	if maxPoints <= 0 {goCover_fbd601c42ea1__23[8] = 1;
		maxPoints = 500
	}
	goCover_fbd601c42ea1__23[4] = 1;interval := windowInterval(window)
	resolution := candleResolution(res)
	source, timeCol, aggregated := candleSeriesSource(window)

	var query string
	if aggregated {goCover_fbd601c42ea1__23[9] = 1;
		query = fmt.Sprintf(`
			WITH joined AS (
				SELECT
					%s AS ts,
					open,
					high,
					low,
					close,
					volume,
					symbol,
					row_number() OVER (ORDER BY %s) AS rn,
					count(*) OVER ()                AS total
				FROM %s
				WHERE %s > now() - interval '%s'
				  AND interval = '1m'
				  AND ($1 = '' OR symbol = $1)
			)
			SELECT ts, open, high, low, close, volume, symbol
			FROM joined
			WHERE total <= $2 OR rn %% GREATEST(1, total / $2) = 0
			ORDER BY ts
			LIMIT $2
		`, timeCol, timeCol, source, timeCol, interval)
	} else{ goCover_fbd601c42ea1__23[10] = 1;{
		query = fmt.Sprintf(`
			WITH seeded AS (
				SELECT
					bucket,
					open,
					high,
					low,
					close,
					volume,
					symbol
				FROM %s
				WHERE %s > now() - interval '%s'
				  AND ($1 = '' OR symbol = $1)
				ORDER BY %s DESC
				LIMIT GREATEST($2 * 24, 600)
			),
			filtered AS (
				SELECT
					time_bucket('%s', bucket) AS grp,
					bucket,
					open,
					high,
					low,
					close,
					volume,
					symbol
				FROM seeded
			),
			ranked AS (
				SELECT
					grp,
					symbol,
					open,
					high,
					low,
					close,
					volume,
					row_number() OVER (PARTITION BY grp, symbol ORDER BY bucket ASC)  AS rn_open,
					row_number() OVER (PARTITION BY grp, symbol ORDER BY bucket DESC) AS rn_close
				FROM filtered
			),
			agg AS (
				SELECT
					grp AS ts,
					symbol,
					MAX(CASE WHEN rn_open = 1 THEN open END)  AS open,
					MAX(high)   AS high,
					MIN(low)    AS low,
					MAX(CASE WHEN rn_close = 1 THEN close END) AS close,
					SUM(volume) AS volume
				FROM ranked
				GROUP BY grp, symbol
			),
			joined AS (
				SELECT
					a.ts,
					a.open,
					a.high,
					a.low,
					a.close,
					a.volume,
					a.symbol,
					row_number() OVER (ORDER BY a.ts) AS rn,
					count(*) OVER ()                  AS total
				FROM agg a
			)
			SELECT ts, open, high, low, close, volume, symbol
			FROM joined
			WHERE total <= $2 OR rn %% GREATEST(1, total / $2) = 0
			ORDER BY ts
			LIMIT $2
		`, source, timeCol, interval, timeCol, resolution)
	}}

	goCover_fbd601c42ea1__23[5] = 1;rows, err := db.Query(ctx, query, symbol, maxPoints)
	if err != nil {goCover_fbd601c42ea1__23[11] = 1;
		return nil, err
	}
	goCover_fbd601c42ea1__23[6] = 1;defer rows.Close()

	out := make([]CandlePoint, 0, maxPoints)
	for rows.Next() {goCover_fbd601c42ea1__23[12] = 1;
		var p CandlePoint
		if err := rows.Scan(&p.Ts, &p.Open, &p.High, &p.Low, &p.Close, &p.Volume, &p.Symbol); err != nil {goCover_fbd601c42ea1__23[14] = 1;
			continue
		}
		goCover_fbd601c42ea1__23[13] = 1;out = append(out, p)
	}
	goCover_fbd601c42ea1__23[7] = 1;return out, nil
}
